# ポートフォリオサイト シンプル化計画

## 概要
参考サイト（ryoppippi.com）を参考に、Resume-Websiteをミニマルなデザインにリファクタリングする。

## 変更方針
- **トップページ**: プロフィール中心のシンプルなランディングページ
- **背景**: P5.jsアニメーション削除 → 単色背景
- **職務経歴**: `/cv` ページへ移動
- **プロジェクト**: `/works` ページへ移動
- **猫キャラ**: ページ最下部に隠し要素として配置

---

## 新しいページ構成

```
src/app/
├── page.tsx          # トップページ（プロフィール + リンク）
├── cv/page.tsx       # 職務経歴ページ [新規]
└── works/page.tsx    # プロジェクト一覧ページ [新規]
```

## トップページのレイアウト

```
+----------------------------------+
|      works  |  cv                |
+----------------------------------+
|                                  |
|     [GitHubアバター画像]          |
|                                  |
|         @setodeve                |
|        Web Engineer              |
|                                  |
|      [GitHub]  [Qiita]           |
|                                  |
+----------------------------------+
|                                  |
|          （余白）                 |
|                                  |
|           🐱                     |
|      (ひっそり猫キャラ)           |
+----------------------------------+
```

---

## 実装ステップ

### Step 1: 新規コンポーネント作成
- [ ] `src/components/Navigation.tsx` - works, cv へのリンク
- [ ] `src/components/Profile.tsx` - GitHubアバター + 名前 + 職業
- [ ] `src/components/SocialLinks.tsx` - GitHub, Qiita ボタン

### Step 2: 新規ページ作成
- [ ] `src/app/cv/page.tsx` - About.tsx の内容を移植
- [ ] `src/app/works/page.tsx` - Project.tsx の内容を移植

### Step 3: トップページ刷新
- [ ] `src/app/page.tsx` を新デザインに書き換え
  - Navigation, Profile, SocialLinks を配置
  - 猫キャラをページ最下部に隠し要素として配置

### Step 4: スタイル整理
- [ ] `src/app/globals.css` から宇宙背景スタイルを削除
- [ ] シンプルな単色背景（#0a0a0a）を設定

### Step 5: クリーンアップ
- [ ] `src/components/P5Background.tsx` を削除
- [ ] `src/components/Header.tsx` を削除（Navigation.tsx に置き換え）
- [ ] `package.json` から `p5`, `@types/p5` を削除
- [ ] 不要なStarryBackground.tsx を削除

### Step 6: 検証
- [ ] `pnpm lint` でエラーがないことを確認
- [ ] `pnpm build` で静的エクスポートが成功することを確認
- [ ] `pnpm test` でテストが通ることを確認

---

## 変更対象ファイル

### 削除
| ファイル | 理由 |
|---------|------|
| `src/components/P5Background.tsx` | 背景アニメーション廃止 |
| `src/components/Header.tsx` | Navigation.tsx に置き換え |
| `src/components/StarryBackground.tsx` | 未使用 |

### 新規作成
| ファイル | 説明 |
|---------|------|
| `src/components/Navigation.tsx` | シンプルなナビゲーション |
| `src/components/Profile.tsx` | プロフィールセクション |
| `src/components/SocialLinks.tsx` | ソーシャルリンクボタン |
| `src/app/cv/page.tsx` | 職務経歴ページ |
| `src/app/works/page.tsx` | プロジェクト一覧ページ |

### 変更
| ファイル | 変更内容 |
|---------|---------|
| `src/app/page.tsx` | 新デザインに全面書き換え |
| `src/app/globals.css` | 背景スタイル整理 |
| `src/components/Cat.tsx` | 隠し要素用にスタイル調整 |
| `package.json` | p5関連パッケージ削除 |

### 維持（移動先で使用）
| ファイル | 移動先 |
|---------|--------|
| `src/components/About.tsx` | `/cv` ページで使用 |
| `src/components/Project.tsx` | `/works` ページで使用 |
| `src/components/SkeletonProjectCard.tsx` | `/works` ページで使用 |

---

## 技術的な注意点

1. **静的エクスポート維持**: `next.config.ts` の `output: "export"` を変更しない
2. **basePath対応**: GitHub Pages用の `/Resume-Website` パスを維持
3. **GitHubアバター**: `https://github.com/setodeve.png` で取得可能
4. **ダークモード**: `globals.css` の `@custom-variant dark` を維持
